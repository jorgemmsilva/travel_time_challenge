<%= form_for(negotiator, format: :js, remote: true) do |f| %>
  

  <div class="form-group">
    <label for="name-input">Name:</label>
    <%= f.text_field :name, class:"form-control", id:"name-input" %>
  </div>

  <% if negotiator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(negotiator.errors.count, "error") %> prohibited this negotiator from being saved:</h2>

      <ul>
      <% negotiator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit class:"btn btn-default" %>
  </div>
<% end %>
